name: Deploy CV Website to Hostinger

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    name: Deploy to Hostinger
    runs-on: ubuntu-latest

    steps:
    - name: 🗂️ Checkout repository
      uses: actions/checkout@v4

    - name: 📋 List files to be deployed
      run: |
        echo "Files that will be deployed:"
        ls -lh *.html *.txt *.json .htaccess 2>/dev/null || echo "Listing all HTML files..."
        find . -maxdepth 1 -type f \( -name "*.html" -o -name "*.txt" -o -name "*.json" -o -name ".htaccess" \)
        echo "---"
        echo "Deployment timestamp: $(date)"

    - name: 📂 Sync files via SFTP
      uses: wlixcc/SFTP-Deploy-Action@v1.2.4
      with:
        server: ${{ secrets.GH_DEPLOY_FTP_SERVER }}
        username: ${{ secrets.GH_DEPLOY_FTP_USERNAME }}
        password: ${{ secrets.GH_DEPLOY_FTP_PASSWORD }}
        port: 22
        local_path: ./
        remote_path: /public_html/
        sftp_only: true
        args: '-o ConnectTimeout=5'
        exclude: |
          **/.git*
          **/.git*/**
          **/node_modules/**
          **/.github/**
          **/README.md
          **/DEPLOYMENT.md
          **/SECURITY.md
          **/package*.json
          **/.DS_Store
          **/Thumbs.db
          **/*.log
          **/.gitignore
          **/CV.txt
          **/MANUAL_DEPLOY.md
          **/*.docx
          **/*.docx.html
          **/*_old.html
          **/*_old.css
          **/*_old.js
          **/resume.html

    - name: ✅ Deployment Complete
      run: |
        echo "🎉 Deployment finished!"
        echo ""
        echo "📍 Check your site:"
        echo "  • https://adrianoroceo.com"
        echo "  • https://adrianoroceo.com/DEPLOYMENT_TEST.html (pink test page)"
        echo ""
        echo "Version: 2.0.0 | Updated: $(date)"